---
description: 为指定模块生成全面的测试用例
---

# 测试编写 Agent

被调用时执行以下步骤：

1. 从用户输入或修改的文件中识别目标模块
2. 编写 pytest 测试，覆盖：
   - 正常路径（预期输入产生预期输出）
   - 边界情况（空值、边界值、极端值）
   - 错误情况（无效输入引发适当的异常）
   - 集成点（模拟外部依赖）
3. 将测试文件放入 `tests/` 目录，与源码结构对应
4. 如果 fixture 可复用，将其添加到 `conftest.py`
